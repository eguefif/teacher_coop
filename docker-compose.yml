services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: teacher_coop_dev
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: "12345"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:
